import { PlaceholderPage } from "../../components/PlaceholderPage"

export default function SetProfileRoute() {
  return (
    <PlaceholderPage
      title="Set Profile"
      description="Collects required profile fields after first sign-in."
      access="Authenticated users with incomplete status."
    />
  )
}
