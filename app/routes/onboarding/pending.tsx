import { PlaceholderPage } from "../../components/PlaceholderPage"

export default function PendingRoute() {
  return (
    <PlaceholderPage
      title="Approval Pending"
      description="Shows the account approval waiting state."
      access="Users with pending status."
    />
  )
}
