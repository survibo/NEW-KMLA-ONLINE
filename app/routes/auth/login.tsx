import { PlaceholderPage } from "../../components/PlaceholderPage"

export default function LoginRoute() {
  return (
    <PlaceholderPage
      title="Login"
      description="Google OAuth sign-in entry point."
      access="Unauthenticated users."
    />
  )
}
